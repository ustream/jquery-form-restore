{
  "name": "jquery-form-restore",
  "version": "1.0.4",
  "description": "Get and restore form values (event checkbox checked state) with jquery",
  "main": "jquery-form-restore.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "$(npm bin)/mocha-phantomjs tests/index.html",
	"build": "$(npm bin)/uglifyjs jquery-form-restore.js -c > dist/jquery-form-restore.min.js"
  },
  "author": "Lajos Koszti",
  "license": "MIT",
  "devDependencies": {
    "chai": "^1.10.0",
    "mocha": "^2.0.1",
    "mocha-phantomjs": "^3.5.2",
    "phantomjs": "^1.9.12",
    "uglify-js": "^2.4.16"
  }
}
